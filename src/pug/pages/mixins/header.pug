// Page Header
mixin header(options)

    header.page-header-ui(class=options.pageHeaderStyle + ' ' + options.pageHeaderBg, style=options.pageHeaderBgImg)

        if block
            block
        else
            p.text-center.py-5 &nbsp;

        //- SVG Border
        include includes/border-options.pug
